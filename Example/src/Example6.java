
public class Example6 {

	public static void main(String[] args) {
          /*응용문제6
           * 
           * 1~10까지 순차적으로 적용하는 반복문이 있습니다.
           * 단, 1~5까지는 모든값을 곱하고,
           * 6~10까지는 모든 값을 더합니다.
           * 두개의 값을 비교하여 더한값이 큰지 곱한값이 큰지를 결과로 출력하시오.
           * 
           * 
           * 
           * */

		/*int a = 1 ;
		int total = 1;
		int total2 = 0;
		int total3;
		do { 
			if (a <=5 ) {
				total *=  a;
			}
			if (a<=6 || a<=10) {
				total2 += a;  
			}
			a++;
		} while (a <= 10);

	      if (total > total2) {
	    	  System.out.println(total);
	      }
	      else {
	    	  System.out.println(total2);
	      }
	    	  */
	/*
		
		int w = 1;
		int total=0; //덧셈
		int total2 = 1; //곳셈
		
		while (w<=10) {
		 if (w<=5) {
			  total2=totla2*w ;
		 }
		 else
			  total1 =total+w ;
	 		w++	
		}
		
		
		if(total2 < total1) {
			System.out.println("더한값이 큽니다.");
		}
		else if (total>total ){
			System.out.println("곱한값이 큽니다.");
		}
		else {
			System.out.println("두 비교값이 같습니다.");
		}
	
	 */
	}

}